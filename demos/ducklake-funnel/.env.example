# DuckLake Funnel Demo — environment variables
# Copy to .env and edit as needed

# Event generation rate (events per second)
EVENTS_PER_SECOND=50

# Catalogue size
PRODUCT_COUNT=20
USER_COUNT=500

# Exposed ports
GRAFANA_PORT=3000
POSTGRES_PORT=5432
